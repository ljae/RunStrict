# /format — Format Dart Code

Format all Dart files using the standard formatter.

## Steps

1. Run `dart format .` in the project root
2. Report which files were changed
3. If any files changed, suggest committing the formatting changes

## Usage

```
/format              # Format entire project
/format lib/         # Format specific directory
```
